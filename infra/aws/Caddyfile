{$DOMAIN} {
	# UI — all routes go to Next.js
	reverse_proxy localhost:3000

	# Health check
	respond /health 200
}

{$DOMAIN}:8443 {
	# API — separate port for direct API access if needed
	reverse_proxy localhost:8000
}
