# Data Directory

This repository intentionally ships without bundled call transcripts or document content.

Use this directory for local development only:

- `data/fake_calls/` for local call JSON fixtures (not committed)
- `data/fake_drive/` for local document fixtures (not committed)

Do not commit customer, prospect, or internal confidential data.
